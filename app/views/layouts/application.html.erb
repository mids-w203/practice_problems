<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Practice Problems | W203</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.3.1/jquery.min.js"></script>
    <link rel="stylesheet" type="text/css" href="https://cdnjs.cloudflare.com/ajax/libs/fomantic-ui/2.8.7/semantic.min.css">
    <script src="https://cdnjs.cloudflare.com/ajax/libs/fomantic-ui/2.8.7/semantic.min.js"></script>

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/simplemde/latest/simplemde.min.css">
    <script src="https://cdn.jsdelivr.net/simplemde/latest/simplemde.min.js"></script>

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>

   <script>
      MathJax = {
        tex: {
          inlineMath: [['$', '$'], ['\\(', '\\)']]
        },
        svg: {
         fontCache: 'global'
        }
      };
    </script>
    <script id="MathJax-script"
    src="https://cdn.jsdelivr.net/npm/mathjax@3/es5/tex-chtml.js">
    </script>
  </head>

  <body>
    <div class="ui container basic segment">
    <div class="ui secondary menu">
      <a class="header item" href="/"><i class="big home icon"></i></a>
      <div class="item">
        <b>w203</b>
      </div>
      
      <div class="ui dropdown item">
          Categories
          <i class="dropdown icon"></i>
          <div class="menu">
            <% Category.all.each do |c| %>
              <%= link_to c, class: "item" do %><%= c.title %><% end %>
            <% end %>
          </div>
        </div>

      <div class="right menu">
        <% if logged_in? %>
        <div class="ui dropdown item"> 
          <img class="ui mini circular image" src="<%= current_user.picture %>">
          <i class="dropdown icon"></i>
          
          <div class="menu">
            <% if current_user.admin? %>
            <%= link_to users_url, class: "item" do %>Users<% end %>
            <% end %>
            <%= link_to logout_url, class: "item" do %>Log out<% end %>
          </div>
        </div>
        <% else %>
        <% end %>
      </div>
    </div>
  </div>
    <div class="ui container">
      <%= yield %>
    </div>

<script>MathJax.typeset()</script>
  </body>
</html>
